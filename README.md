# Getting started

-- Check for the Python version "python --version"
-- Create a virtual environment "python -m venv <name_of_the_venv>"
-- Activate the virtual environment "source venv/Scripts/activate"
-- # 1. Upgrade the package installer tool "python -m pip install --upgrade pip"
-- # 2. Install Playwright for Python "pip install playwright"
-- # 3. Download the required browser binaries (Chromium, Firefox, WebKit) "playwright install"
